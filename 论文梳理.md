# 电力设备健康管控智能体设备预警系统研究与实现

> 预警的核心在于"前瞻性"——通过对监测数据的趋势分析与智能预测，实现从"事后处理"到"事前预防"的转变。
>
> **预测时间窗口说明**：考虑到变压器气体浓度变化通常较缓慢（以天为单位），且本课题重点在于验证"预测+预警"的完整流程而非追求超前预测时长，本文设定预测目标为未来 1-3 天的状态变化。这一时间窗口已能为运维人员提供充足的预警提前量，也符合变压器运维的实际需求。

---

## 一、论文目标

基于大语言模型智能体的电力变压器智能预警系统，具体目标包括：

1. 实现基于 LSTM 多输出回归的变压器七种 DGA 气体同步预测，预测时间窗口为未来 1-3 天
2. 能检测数据中的异常信号和趋势异常
3. 能决策什么时候、什么等级触发预警
4. 能控制误报率，避免"狼来了"
5. 能让 AI 自动执行预警决策流程
6. 能通过监控大屏直观展示预警结果

---

## 二、技术路线

### 2.1 技术选型（结合自身情况）

| 层级     | 技术选型                             | 选型理由                            |
| -------- | ------------------------------------ | ----------------------------------- |
| 算法层   | pandas + sklearn + Keras(TensorFlow) | 调包为主，门槛较低                  |
| 预测模型 | LSTM（套模板）+ ARIMA（基线）        | 不做 Transformer，难度可控          |
| AI 层    | LangChain + 通义千问                 | Agent 框架 + 国产大模型（免费额度） |
| 后端     | FastAPI                              | 对前端工程师友好，自带 Swagger 文档 |
| 前端     | Vue3 + Element Plus + ECharts        | 专业朴素，发挥前端优势              |
| 数据库   | MySQL                                | 业务存储                            |

### 2.2 监测指标体系

本文监测范围聚焦三类核心指标，其余指标（局放、振动、噪声等）受限于公开数据集获取难度，暂不纳入本系统。

| DGA 气体<br/>（直接检测） | 衍生指标<br/>（特征工程计算） | 运行工况<br/>（辅助判断） |
| :-----------------------: | :---------------------------: | :-----------------------: |
|          H₂　CH₄          |             总烃              |      油温 / 温升速率      |
|        C₂H₆　C₂H₄         |           产气速率            |         负载电流          |
|           C₂H₂            |           三比值法            |         环境温度          |
|          CO　CO₂          |            CH₄/H₂             |                           |
|                           |           C₂H₂/C₂H₄           |                           |
|                           |           C₂H₄/C₂H₆           |                           |

> **核心地位**：DGA 七种气体是 LSTM 预测目标与预警核心依据
>
> **辅助地位**：运行工况为规则引擎提供上下文，不直接参与预测

### 2.3 系统采用四层架构设计

|    层级    | 说明                        | 核心内容                                                          |
| :--------: | --------------------------- | ----------------------------------------------------------------- |
| **应用层** | 可视化大屏                  | 监控 + 预警展示                                                   |
| **智能层** | 基于 LangChain 的预警 Agent | Agent 主体（决策引擎）+ Tool 工具集（4 个）+ LLM 接口（通义千问） |
| **算法层** | 核心算法                    | 预测（预测未来）+ 检测（发现异常）+ 决策（规则+分级+告警）        |
| **数据层** | 数据基础                    | 数据采集与存储                                                    |

### 2.4 四大核心预警能力

| 能力        | 通俗理解             | 实现方式             | 对应模块 |
| ----------- | -------------------- | -------------------- | :------: |
| 能看见      | 发现异常信号         | 异常检测（3 种方法） |  模块 3  |
| 能预测 ⭐   | 预测未来走势         | LSTM 多输出回归      |  模块 4  |
| 能决策 ⭐   | 判断何时报警、报几级 | 规则引擎 + 四级分级  |  模块 5  |
| 能自动化 ⭐ | AI 自动执行预警流程  | LangChain Agent      |  模块 6  |

---

## 三、研究内容与工作量规划

本课题共规划 **7 个核心模块**，涵盖从数据处理到系统部署的完整链路。

---

### 模块 1：数据采集

**研究内容：**

1. **数据采集**：从 GitHub、Kaggle 下载变压器公开数据集（IEC TC 10 等）
2. **数据清洗**：处理缺失值、删除明显错误的数据

> ### ⚡ 做什么（实施重点）
>
> 1. 下载公开变压器数据集
> 2. 简单清洗（删除缺失值）
> 3. 数据库建表存储

**不做：**

- ❌ 不做复杂的"状态演化数据生成"

**预期成果**：构建包含充足样本的变压器监测数据集

---

### 模块 2：数据加工

**研究内容：**

1. **基础特征提取**：DGA 七种气体浓度、总烃、气体比值（CH₄/H₂、C₂H₂/C₂H₄ 等）、油温、负载、环境温度

2. **时序特征构建（预警专属）** ⭐：滑动窗口统计（过去 7 天的均值、方差、最大值、最小值）、趋势斜率（涨跌方向与速度）

3. **变化率特征（预警关键）** ⭐⭐⭐：产气速率、温升速率 —— 即使当前值没超标，涨得快就要报警

4. **数据归一化**：让所有特征的量级统一，方便模型处理

> ### ⚡ 做什么（实施重点）
>
> - DGA 气体浓度、总烃、气体比值（简单计算）
> - 产气速率、温升速率、负载、环境温度（核心，一定要有）
> - 滑动窗口的均值、最大值
> - 数据归一化

**不做：**

- ❌ 不做复杂的统计特征工程
- ❌ 不做 AHP 层次分析

**预期成果**：一套为预警量身定制的特征工程模块

---

### 模块 3：异常检测

**研究内容**：发现"当前"数据中的异常信号，特别是"时序上的异常"（动态异常检测）。

- **基础异常检测**：阈值法（参考国标 DL/T 722）、Isolation Forest（孤立森林）
- **时序异常检测**：突变检测、趋势异常、周期异常
- **多方法对比实验**：对比准确率、召回率、误报率

> ### ⚡ 做什么（只做 3 种）
>
> 1. 阈值法（最简单）
> 2. IEC 三比值法（论文必须有的国标方法）
> 3. Isolation Forest（sklearn 一行代码搞定）
>
> **对比指标**：准确率、召回率、误报率

**不做：**

- ❌ 不做 Autoencoder（太难，用不着）
- ❌ 不做关联异常检测

**预期成果**：完成多方法对比实验报告，形成异常检测的最佳实践方案

---

### 模块 4：趋势预测

**研究内容**：用过去 30 天的 DGA 监测数据，预测未来 1-3 天的七种特征气体浓度。采用"传统方法作基线 + 深度学习作主力"的对比实验设计。

#### 基线模型（对照组）：ARIMA

- 对每种气体独立建模（共训练 7 个 ARIMA），作为传统统计学基线
- 为 LSTM 提供性能参照

#### 深度学习模型（主力）：LSTM 多输出回归

- **输入**：过去 30 天的七种气体浓度时序数据
- **输出**：未来 1 天的七种气体浓度预测值（训练时单步训练）
- **推理**：滚动预测，用预测值回填输入窗，实现 1-3 天多步预测
- **核心设计**：`Sequential([LSTM(64), Dense(7)])`，单模型同时输出 7 个预测值
- **框架**：Keras（TensorFlow），代码简洁、调试友好

#### 七种气体同步建模的科学价值 ⭐

变压器内部不同故障会产生不同气体组合（如 H₂+CH₄ 指示局部放电，C₂H₂+C₂H₄ 指示高温过热）。本文通过共享隐藏层隐式学习气体间耦合关系，更符合故障机理，也是 LSTM 相对 ARIMA（必须独立建模 7 次）的核心优势。

> ### ⚡ 做什么
>
> 1. ARIMA 基线：statsmodels 库，对 7 种气体各跑一次（for 循环）
> 2. LSTM 多输出回归：`Sequential([LSTM(64), Dense(7)])`
> 3. 滑窗造样本：过去 30 天 → 预测第 31 天（单步训练）
> 4. 滚动推理：用预测值回填，迭代 2 次得到第 32、33 天
> 5. 数据归一化：MinMaxScaler，训练后反归一化还原真实值
> 6. 对比实验：ARIMA vs LSTM，MAE/RMSE/MAPE 三指标
> 7. 可视化：各气体 1-3 天预测趋势曲线（7 张子图）、误差分布直方图

**不做：**

- ❌ 不做 Transformer、Informer 等复杂架构
- ❌ 不做长周期多步预测（7/15/30 天），仅做 1-3 天
- ❌ 不做 GRU 对比（LSTM 已足够）
- ❌ 不做置信区间估计
- ❌ 不做 Prophet（ARIMA 作为基线已足够）

**技术兜底方案：**

- 优先用 Keras LSTM，训 3-5 天能跑通就是胜利
- 如果 LSTM 效果不佳，论文改写为"ARIMA vs LSTM 对比实验，实验表明传统方法在该数据集上更稳健"
- 如果 LSTM 完全不收敛，降级为纯 ARIMA 预测，重心转向规则预警与可视化系统

#### 举个例子

**输入**：2023-01-01 至 2023-01-30 的七种气体每日浓度（真实值）

| 日期  | H₂  | CH₄ | C₂H₂ | C₂H₄ | C₂H₆ | CO  | CO₂ |
| ----- | :-: | :-: | :--: | :--: | :--: | :-: | :-: |
| 01-01 | 12  |  5  | 0.5  |  8   |  2   | 50  | 300 |
| 01-02 | 13  |  6  | 0.6  |  9   |  2   | 52  | 310 |
| ...   | ... | ... | ...  | ...  | ...  | ... | ... |
| 01-30 | 15  |  8  | 0.8  |  12  |  3   | 60  | 350 |

**滚动预测过程：**

- 第 1 步：`[真实 Day1-30] → LSTM → 预测 Day31 = H₂:16.2, CH₄:8.5, ...`
- 第 2 步：`[真实 Day2-30 + 预测 Day31] → LSTM → 预测 Day32 = H₂:17.1, ...`
- 第 3 步：`[真实 Day3-30 + 预测 Day31,32] → LSTM → 预测 Day33 = H₂:18.3, ...`

> 注：每次推理只输出 1 天，通过 3 次迭代实现 1-3 天多步预测。训练简单（只需训练单步预测），推理灵活。

**预期成果：**

- LSTM 多输出回归的七种 DGA 气体 1-3 天滚动预测
- ARIMA vs LSTM 对比实验报告（含 7 种气体精度对比表）
- 训练好的 LSTM 模型文件（`.h5`）
- 预测结果可视化图表（7 种气体 3 天预测趋势曲线）

**论文写法提示（未来工作章节）：**

> 本文聚焦于 1-3 天短期预测与 LSTM 基础架构验证，未来可探索 Transformer、Informer 等更先进的时序预测架构，以及更长周期的多步滚动预测。

---

### 模块 5：预警决策系统

**研究内容**：预警决策 = 从预测结果 → 判断是否报警 + 报几级警 + 如何触发

**研究核心**：什么条件下报警？报几级？怎么减少误报？报给谁？

#### 1. 预警规则引擎设计 ⭐

| 规则类型 | 说明           | 举例                         |
| -------- | -------------- | ---------------------------- |
| 硬规则   | 基于国标阈值   | C₂H₂ > 5 μL/L 立即报警       |
| 软规则   | 基于预测结果   | 预测 1-3 天内超标 → 预警     |
| 组合规则 | 基于多指标关联 | 气体涨快 + 温度高 → 综合预警 |

做成可配置的引擎，可以灵活调整。

#### 2. 四级预警分级机制 ⭐

| 等级            | 触发条件               | 响应要求  |
| --------------- | ---------------------- | --------- |
| 🔴 红色（紧急） | 已超标 或 24h 内必超标 | 立即响应  |
| 🟠 橙色（重要） | 1-3 天内可能超标       | 24 小时内 |
| 🟡 黄色（一般） | 趋势异常上升           | 加强监测  |
| 🔵 蓝色（提示） | 轻微波动               | 日常关注  |

#### 3. 误报率控制（规则层面）

- 持续性判断：异常需连续 N 次触发才报警（过滤噪声尖峰）
- 预警去重：同一预警 24 小时内不重复推送

#### 4. 预警触发与推送机制

- 存入数据库（warning 表）
- 前端实时展示（模块 7 大屏）

> ### ⚡ 做什么（实施重点）
>
> 1. 预警规则引擎（if-then 规则，Python 字典实现）
> 2. 四级预警分级（红橙黄蓝）
> 3. 规则引擎实现：规则加载、规则触发、等级综合判定
> 4. 误报率控制：持续性判断 + 预警去重
> 5. 规则验证：历史数据回测、有效性分析
> 6. 预警推送：存数据库 + 前端展示

**不做：**

- ❌ 不做动态阈值学习（写进未来工作）
- ❌ 不做多模型投票（写进未来工作）
- ❌ 不做置信度评估（写进未来工作）

**【扩展研究方向】（论文未来工作章节）**

- 动态阈值自适应学习
- 多模型投票机制
- 预警置信度量化评估

**预期成果：**

- 完整的预警决策系统
- 规则库文档
- 历史数据回测验证报告

---

### 模块 6：基于 LangChain 的预警智能体（Agent）

**模块 6 核心定位（三句话）：**

1. **Agent 角色**：基于 LangChain + ReAct 推理模式实现"数据→检测→预测→决策→通知"的自动化预警流程
2. **LLM 角色**：通义千问作为 Agent 大脑，通过 Prompt 引导按固定流程执行，不做开放式自主决策
3. **设计原则**：4 个核心工具 + Prompt 固定流程引导，Agent 卡死时降级为纯规则引擎

**研究内容**：基于 LangChain 框架构建变压器预警智能体，通过工具调用和 ReAct 推理，将 LSTM 预测、异常检测、规则引擎、通知生成整合为端到端的自动化预警决策流程。

#### Agent 核心组成

- **LLM 大脑**：通义千问（qwen-turbo），通过 LangChain ChatTongyi 接入
- **工具集（4 个）**：
  - `get_today_data()` —— 查询最新监测数据
  - `detect_anomaly()` —— 调用异常检测（阈值法 + 三比值法 + Isolation Forest）
  - `lstm_predict()` —— 调用 LSTM 预测未来 1-3 天
  - `check_warning()` —— 调用规则引擎判定预警等级
- **ReAct 推理模式**：思考(Thought) → 行动(Action) → 观察(Observation) 循环
- **Prompt 模板**：固定流程引导，Agent 按预设步骤执行
- **AgentExecutor**：LangChain 执行引擎

#### Agent 工作流程（Prompt 中写死的 5 步）

```
触发：定时任务（每天定时执行一次）
        ↓
Step 1：调用 get_today_data 获取最新数据
        ↓
Step 2：调用 detect_anomaly 进行异常检测
        ↓
Step 3：调用 lstm_predict 预测未来 1-3 天趋势
        ↓
Step 4：调用 check_warning 进行预警判定
        ↓
Step 5：LLM 基于结果生成预警通知文本 + 复核意见
        ↓
通知入库 + 前端展示
```

#### Prompt 模板示例（节选）

```
你是变压器预警专家。请严格按以下 5 步执行：

Step 1: 调用 get_today_data 获取最新数据
Step 2: 调用 detect_anomaly 进行异常检测
Step 3: 调用 lstm_predict 预测未来 1-3 天
Step 4: 调用 check_warning 判定预警等级
Step 5: 综合结果，按标准格式生成预警通知

注意：必须按顺序执行，不要跳步骤。

通知格式模板：

🚨 【{level}预警】{device_name}
📊 当前状态：...
🔮 预测趋势：...
⚖️ 触发规则：...
```

> ### ⚡ 做什么
>
> 1. 基于 LangChain 搭建 Agent（先跑通 hello world，确认环境可用）
> 2. 封装 4 个核心工具，通知生成不单独封装
> 3. 设计 ReAct 模式 Prompt 模板（写死执行步骤）
> 4. 实现 Agent 自动化执行（AgentExecutor + 定时调度）
> 5. 异常处理：Agent 调用失败时降级为纯规则引擎 Pipeline
> 6. 前后端对接：通知文本存入数据库，前端模块 7 展示

**代码量预估：**

| 部分                                                |    行数     |
| --------------------------------------------------- | :---------: |
| 4 个 Tool 封装（@tool decorator + pydantic schema） |   ~80 行    |
| Prompt 模板                                         |   ~30 行    |
| Agent 初始化与执行                                  |   ~40 行    |
| 异常降级处理                                        |   ~20 行    |
| **总计**                                            | **~170 行** |

**不做：**

- ❌ 不单独封装"通知生成工具"（由 LLM 在 Final Answer 中直接输出）
- ❌ 不做自由的 ReAct 推理
- ❌ 不做复杂的 Memory 管理、会话管理
- ❌ 不做多 Agent 协作
- ❌ 不引入 RAG 知识库增强
  > LLM 仅承担"复核"和"通知生成"，规则引擎已提供完整领域知识。RAG 可作为未来工作。

**技术兜底方案：**

- 优先用 LangChain + ReAct Agent，第 12 周前必须跑通基础 demo
- 如果 Agent 不稳定，通过 Prompt 加强流程约束
- **极端情况**：砍掉 LangChain，退回 Pipeline + API 调用

> 本文基于 LangChain Agent 实现预警决策自动化，未来可探索 Agent 自主决策等更高级范式。

**预期成果：**

- 完整的 LangChain Agent 实现（约 170 行核心代码）
- 4 个预警工具的封装
- ReAct 推理模式下的自动化预警决策
- 标准格式预警通知自动生成
- 每日定时任务自动执行

---

### 模块 7：预警可视化平台开发

**研究内容：**

- 监控大屏（ECharts）
- 预警展示模块 ⭐（预警列表、等级、历史）
- 预测展示模块 ⭐（七种气体 1-3 天预测趋势曲线）
- Agent 执行可视化 ⭐
  - 实时展示 Agent 5 步执行状态（成功/失败/进行中）
  - 展示每步工具调用的输入输出
  - 展示 Agent 的"思考过程"（Thought）
  - 失败时高亮显示并说明原因
- AI 预警通知展示（LLM 生成的复核意见与通知文本）
- 后端接口

**预期成果**：完整可演示的预警系统

---

## 四、创新点

### 创新点 1：变压器多方法异常检测对比研究

实现阈值法、三比值法（DL/T 722-2014）、Isolation Forest 三种方法的系统对比，给出各方法在变压器预警场景下的适用条件与性能差异。

### 创新点 2：基于 LSTM 与 ARIMA 的 DGA 气体趋势预测对比研究 ⭐

区别于传统对每种气体独立建模的方法，本文采用单个 LSTM 模型同时预测七种气体浓度，通过共享隐藏层隐式学习气体间的物理化学耦合关系（如 H₂+CH₄ 指示局部放电、C₂H₂+C₂H₄ 指示高温过热）。实验对比显示：在小样本（单设备 360 天）、高波动的 DGA 合成数据上，经典统计方法 ARIMA 较深度学习 LSTM 单步预测精度略优（总体 MAE：ARIMA 148 vs LSTM 160 μL/L，逐气体 ARIMA 胜 6/7；D-044 全链路重跑），主要原因在于 LSTM 对训练样本量有更高要求、且 MinMax 归一化在极端值跨度大的序列上压缩了正常变化的分辨率。本文的价值在于据实对比，为变压器预警系统在不同数据规模下的算法选型提供实证依据，体现了工程务实的研究态度。

### 创新点 3：基于 LangChain Agent 的预警决策智能化 ⭐⭐⭐ 最大创新

本文基于 LangChain 框架构建变压器预警智能体，通过工具调用机制和 ReAct 推理模式，实现了"数据→异常检测→预测→决策→通知"的端到端自动化预警流程。Agent 严格聚焦于预警边界（4 个工具 + Prompt 固定流程引导），不越界做评估或诊断，体现了清晰的研究定位与务实的工程实现。

---

## 五、进度安排

|  阶段   |    时间     | 任务                                                           | 交付物                                  |
| :-----: | :---------: | :------------------------------------------------------------- | :-------------------------------------- |
|    1    |  第 1-2 周  | 调研 + 论文梳理初稿 + 可视化框架搭建                           | 论文梳理初稿 + 大屏空壳                 |
|    2    |  第 3-4 周  | ⭐ 开题报告 + 论文梳理定稿 + 制作PPT + 开题答辩 + 优化前端系统 | 开题报告 + PPT + 系统展示               |
|    3    |   第 6 周   | 数据集预下载 + 特征工程 + 可视化初版对接                       | 700+ 条数据集 + 特征模块 + 大屏数据联调 |
|    4    |   第 7 周   | 异常检测                                                       | 检测模块                                |
| ⭐ 中期 |   第 8 周   | ⭐ 中期检查                                                    | 完成基础工作                            |
|    5    | 第 9-10 周  | LSTM 预测（核心）                                              | 预测模型                                |
|    6    | 第 11-12 周 | 预警决策（核心）                                               | 决策系统                                |
|    7    |  第 13 周   | LangChain Agent + 完整集成                                     | AI 智能体 + 全链路贯通                  |
|    8    |  第 14 周   | 系统完善 + 论文                                                | 可演示系统 + 论文初稿                   |
|    9    |  第 15 周   | 论文 + 答辩准备                                                | 最终答辩                                |

---

> 时间和基础有一定限制。经过评估，我打算聚焦以下几个方面：
>
> 1. **核心实现**：基于 LSTM 的时序预测 + 规则化预警决策 + LangChain Agent 自动化预警
> 2. **技术选型**：选择 LSTM 而非 Transformer，选择 LangChain 轻量 Agent（4 工具 + Prompt 固定流程）而非复杂 Agent 架构，确保能扎实实现
> 3. **聚焦工程**：重点做好完整的系统、可演示的大屏、清晰的论文
> 4. **合理取舍**：Transformer、动态阈值等前沿技术放在"未来工作"章节
>
> 这样既能体现预警系统的核心价值，又能在我能力范围内做扎实，请老师指导。

---

## 六、实施期修订（2026-06-03 起）

> 本节记录正式开发阶段（第 6 周起）相对于上述初版规划的修订。修订源于真实数据集情况与工程实操中的发现，**不改变论文的核心目标、创新点与系统架构**，仅在数据来源、字段范围、技术细节层面做务实调整。详细决策见 `docs/05-decisions-log.md`。

### 修订 1：数据库 MySQL → SQLite（技术选型）

- **背景**：评估部署成本与风险
- **修订**：将"业务存储"由 MySQL 改为 SQLite
- **理由**：本项目数据量级（万行）下 SQLite 完全胜任；零安装、零运维、答辩演示风险更低
- **代码兼容**：通过 SQLAlchemy ORM 编写，可平滑迁移至 MySQL
- **影响**：仅影响第 33 行选型表，不影响系统架构与功能

### 修订 2：数据集为快照型 → 引入"分布保持的时序构造"（替代原"不做状态演化数据生成"）

- **背景**：选定的公开数据集（`FinalDataSet_DGA.xlsx`，743 行）为快照型，无时间戳；无法直接用于 LSTM 时序预测
- **修订**：
  - 原模块 1 中"❌ 不做复杂的状态演化数据生成"调整为：**基于真实样本分布特征，通过分布保持的轻量扰动构造时序序列**
  - 本质是"给真实快照样本附加时间维度"，而非"凭空生成数据"
  - 是数据增强、半合成数据集的常见做法
- **论文表述**：在数据章节坦诚说明数据来源与处理方法，重申本课题定位为"验证预测+预警完整流程"
- **影响**：模块 1、模块 4 的数据来源说明需相应调整

### 修订 3：原始 `Fault` 列不可作为 ground truth → 用 IEC 60599 自动打标作为参照

- **背景**：EDA 发现 743 行中 **97.6% 的 `Fault` 列为 `'NA'`**（未标注），有效标签仅 18 行，无法支撑统计意义上的算法评估
- **修订**：
  - **采用 IEC 60599 国际标准三比值法对全量样本进行自动诊断**，得到"参考标签"
  - 阈值法、Isolation Forest 等其他异常检测方法以 IEC 自动诊断结果为基准做横向对比，评估各方法**与国际标准的一致性与互补性**
  - 该思路并非循环论证：被评估的是"阈值法/IF"，IEC 是国际标准；以标准为参照评估非标准方法，方法论成立
- **EDA 实测结果**：
  - 异常样本 261（35.1%）+ 正常样本 229（30.8%）→ ground truth 充足
  - Undetermined 252（33.9%）→ 反映 IEC 三比值法的固有局限，可作论文中"单一方法局限性"的论据
  - 与原始有限的 18 个标签做交叉验证，整体一致
- **影响**：模块 3、模块 5 的算法评估方法论调整为"以 IEC 国标为参照"

### 修订 4：油温/负载/环境温度等工况数据 → 模拟生成

- **背景**：选定的公开数据集仅含 DGA 气体浓度，**不含工况数据**（论文模块 2 要求的油温、温升速率、负载、环境温度均缺失）
- **修订**：
  - 油温：基础温度 + 日波动 + 负载相关项
  - 负载电流:工作日/周末模式 + 正弦周期波动
  - 环境温度：季节性正弦 + 日内波动
  - 上述模拟参数取符合工程经验的合理范围
- **论文表述**：在数据章节明确说明工况数据为模拟生成，及其参数依据
- **影响**：模块 2 特征工程的工况相关项基于模拟数据展开

### 修订 5：H₂O 列删除

- **背景**：EDA 发现 H₂O 列 100% 缺失
- **修订**:从特征列中剔除
- **影响**:无（H₂O 不在论文要求的 7 种核心 DGA 气体内）

### 修订 6:CO/CO₂ 部分缺失的处理预案

- **背景**：CO 缺失 21.8%、CO₂ 缺失 31.4%（其余 5 种核心气体完整）
- **修订**:在时序合成阶段为所有 7 种气体生成完整序列；保留"7 气体同步预测"的论文叙事
- **降级预案**:若合成质量不足导致 LSTM 训练 CO/CO₂ 困难,可降级为"5 气体同步 LSTM + CO/CO₂ 独立 ARIMA",论文叙事相应改为"以 5 气体同步预测为主,CO/CO₂ 独立建模"
- **影响**：模块 4 视实验结果决定最终预测目标的气体数量

### 修订 7:模块 4 预测架构显式化为「离线训练 + 在线推理」

- **背景**:论文模块 4 把训练好的 LSTM 模型(`.h5`)列为交付产物,隐含「训练产出持久化模型 → 加载推理」的部署架构,但原文未显式描述这一分离
- **修订**:明确区分两层 —— ①**离线训练**(脚本一次性跑:加载时序 → fit 归一化器 + 训练 → 落盘 `.h5` + scaler 参数)②**在线推理**(后端 API 进程启动时加载一次模型,每次请求只做滚动 3 步预测,毫秒级返回)。该架构贴近工业部署实际,且守住「算法层不依赖 DB/HTTP」的工程分层
- **环境**:为装 TensorFlow 2.16.2 + Keras 3,后端 Python 由 3.9.6 整体升级到 3.11.12(Homebrew `python@3.11`,本机为 Intel Mac 故用标准 `tensorflow` 而非 `tensorflow-macos`)。模型小(`LSTM(64)`),CPU 训练足够,不依赖 GPU
- **影响**:无叙事变更 —— 框架(Keras/TensorFlow)、模型结构(`Sequential([LSTM(64), Dense(7)])`)、`.h5` 产物均与原文一致,仅把隐含架构说清楚

---

### 修订 8:LSTM vs ARIMA 实测——ARIMA 在本合成数据集上更优(预测叙事调整)

> ⚠️ **本节数字为旧数据(D-029)的历史记录;D-044 全链路重跑后已更新为:总体 MAE ARIMA 148.3 vs LSTM 160.4、逐气体 ARIMA 胜 6/7(仅 CO 一项 LSTM 略优),结论方向(ARIMA 更优)不变但程度由「全面碾压」减弱为「略优」。最新数字见修订 16 / D-044,下表保留作演进留痕,请勿直接引用。**

- **背景**:模块 4 阶段 B 完成,在验证段(后 20%,42 个目标日)做单步 walk-forward 对比 LSTM 与 ARIMA(详见 docs D-028)
- **实测结果(旧数据,7 气体全部 ARIMA 胜)**:

  | 气体 | LSTM MAE | ARIMA MAE | 胜者 |
  | :--- | ---: | ---: | :---: |
  | h2 | 22.62 | 18.83 | ARIMA |
  | ch4 | 20.43 | 0.77 | ARIMA |
  | c2h4 | 83.56 | 5.49 | ARIMA |
  | c2h6 | 55.08 | 22.10 | ARIMA |
  | c2h2 | 14.85 | 1.27 | ARIMA |
  | co | 370.61 | 190.78 | ARIMA |
  | co2 | 5983.70 | 1869.35 | ARIMA |
  | **总体均值** | **935.83** | **301.23** | **ARIMA** |

- **原因分析(据实写,不掩饰)**:① 合成序列日间波动极大、7 气体跨度巨大(co2 因异常段最高 ~60000),MinMax 全域归一把 normal 变化压成极窄子带,LSTM 单步误差反归一化后在原始 μL/L 放大;② 单设备 360 天、滑窗后训练样本仅 ~258 个,对 LSTM 偏少;③ ARIMA 每步用全量历史重拟合 + 一阶差分,对这种「均值回复 + 强噪声」序列反而更稳
- **叙事调整(承 D-027 兜底预案,now 转正)**:原创新点 2 措辞「提升了预测精度」与实测冲突。**改为对比研究叙事**:不再宣称 LSTM 精度占优,而是「在小样本、高波动的单设备 DGA 合成数据上,经典统计方法 ARIMA 较深度学习 LSTM 更稳健;LSTM 的多输出共享表示在数据量受限时优势未能兑现」。这本身是有价值的实证结论(选型应匹配数据规模),且与创新点 1「多方法对比研究」的方法论一脉相承
- **✅ 已拍板(2026-06-11,方案 A)**:创新点 2 正文已重写为对比研究叙事——标题改「基于 LSTM 与 ARIMA 的 DGA 气体趋势预测对比研究」,据实承认 ARIMA 更优 + 说清原因(样本量/归一化分辨率),价值落在「为不同数据规模下的算法选型提供实证依据」。保留 LSTM 多输出共享表示技术点
- **产物**:`scripts/compare_predict.py`(MAE/RMSE/MAPE 对比表)+ `notebooks/figures/predict_compare.png`(7 子图真值 vs 两法)+ `data/predict_eval.json`(前端数据源)
- **前端已据实呈现(D-031)**:PredictionView 据实改形——KPI/柱状图/曲线全部翻转为「ARIMA 胜」,接 `/api/predict/compare` 真值,新增「为什么 ARIMA 更优」原因块,删除原杜撰的「LSTM 创新点⭐」误导内容,摘掉「规划中」横幅。大屏与论文叙事一致(对比研究、据实承认 LSTM 局限)
- **下游应用(D-032)**:实测结论已被模块 5 采纳——预警「软规则(基于预测)」的预测源定为 ARIMA(而非 LSTM),即「用对比实验中更稳健的模型做预警决策依据」,使模块 4 的实证结论与模块 5 的工程实现自洽。论文模块 5 叙事本身不变(软规则定义不依赖具体模型)

---

### 修订 9:预警回测基准统一为 fault_state + 误报校准的诚实记录(模块 5)

> ⚠️ **本节误报校准结论已被 D-044(修订 16)更新,以下为旧记录**:① CO=900 分位校准方案已**废除**——CO/CO₂ 改为 §10.2.3.1 CO₂/CO 比值判据(删 CO 绝对硬规则);② 重跑后误报率为 **73.2%**(非 70%);③ 「剩余误报=弱可分」的归因已修正为「早期/轻微异常边界样本 + 预警预测提前量」(数据在表3 打标下可分性合理)。回测基准统一为 fault_state 这一点仍有效。最新见修订 16 / D-044。

- **背景**:模块 5 历史回测落地(D-033)。涉及两处与早期描述的演进
- **回测基准 IEC → fault_state**:修订 3 当时定「模块 3、模块 5 评估以 IEC 国标为参照」,但 D-020 已把检测对比基准改为合成真值 fault_state(IEC 是内部打标工具,以其为基准属「自己跟自己比」)。预警回测同理统一为 fault_state,使检测与预警两模块同一基准。论文阶段五「以 IEC is_abnormal 算 TP/FP/FN」措辞应改为「以合成真值 fault_state」
- **误报校准 + 诚实发现**:首跑回测误报率 93%,根因是硬规则 CO 国标注意值(300)在本合成数据正常态 38% 天数即触发(CO 合成基线偏高)。经两项处理——① CO 预警阈值按设备正常态 95 分位校准为 900(国标允许按历史基线调,且只作用于预警引擎、不动检测国标口径)② 接入误报控制的持续性判断(连续 2 次才报)——误报率降至 70%、TN 翻 4 倍。**剩余 70% 误报为数据固有的弱可分性**(CO 正常/异常分布几乎重叠;模块 3 检测三方法准确率本就 60-80%),如实记录、不再硬调阈值过拟合。与修订 8(ARIMA 更优)、修订 3(IEC 局限)一脉相承,均为「据实呈现合成数据约束下的方法表现」
- **产物**:`scripts/backtest.py` + `data/warning_backtest.json` + `notebooks/figures/warning_backtest.png`(回测验证报告素材)

---

### 修订 10:Agent 执行可视化形态明确——嵌入预警工单详情,点单追溯(模块 7)

- **背景**:论文模块 7 列「Agent 执行可视化 ⭐」为独立展示模块,但未明确它在大屏哪个页面、以什么形态呈现。实施期开发计划曾随手写「接入 AnalysisView」,经核查无依据且与 Analysis「三层指标看板」定位冲突
- **明确(D-035)**:Agent 执行可视化**嵌入预警工单(AlertsView)详情**,与工单**一对一绑定**——点某条预警工单 → 右侧展示「该预警背后 Agent 的 5 步 ReAct 推理轨迹」(Thought/Action/Observation + 状态 + 失败降级高亮)。这比独立页更有产品价值:体现「每条预警都可追溯它是如何被智能体推导出来的」,呼应最大创新点(Agent 端到端串联)
- **形态**:`AgentTrace.vue` 组件(时间线 + 三要素展开 + 状态条)。模块 6 开发前用示意数据布局(显眼标注「模块6·第13周·示意」),开发后接 `/api/agent/run` 真实 trace。Step3 预测据 D-032 用 ARIMA(非论文原写 lstm_predict)
- **影响**:论文模块 7 叙事无变更,仅明确了 Agent 可视化的承载页与交互形态

---

### 修订 11:AnalysisView 三层数据体系由静态原型改为据实接真,补第2层特征工程展示(模块 7)

- **背景**:论文模块 7 画 AnalysisView 为「三层数据体系」(DGA 七气体 / 衍生指标 / 运行工况),开题阶段先用静态 UI 原型搭出形态。算法落地后该页未清理,且 Tier2 原稿写死 `021 → 低温过热 (<300℃)`,把 IEC 三比值法的**具体故障类型暴露在业务页面**,违反系统刚性边界(IEC 仅内部用)
- **修订(D-038)**:保留「三层数据体系」原始意图(本研究监测对象的数据基础叙事,区别于 DetectionView 的算法评测),三层全部**据实接真**:
  - **第1层 DGA 七气体(直接检测)**:LSTM 预测目标 + 预警核心依据(核心地位)
  - **第2层 衍生指标(特征工程)**:总烃 / 产气速率 / 三比值(CH₄/H₂、C₂H₂/C₂H₄、C₂H₄/C₂H₆)——补上模块 2 特征工程产物此前前端无集中展示的缺口
  - **第3层 运行工况(辅助判断)**:油温/温升速率、负载电流、环境温度——为规则引擎提供上下文,不直接参与预测(辅助地位)
- **边界自洽**:三比值**只呈现数值**(中性衍生指标),**不展示 IEC 编码与故障类型**(删 `021→低温过热`);页面无故障类型/健康评分/运维建议
- **影响**:模块 7「三层数据体系」叙事不变,仅由静态原型改为据实接真并补第2层展示;原三层与 Dashboard 的气体/工况交集属角度不同(Dashboard=运行总览,Analysis=数据体系全貌)。模块 7 各页全部据实接真,无「规划中」横幅(仅 Agent 轨迹/AI 通知挂示意标,待模块 6)。承 D-030 前端视图非刚性原则

---

### 修订 12:数据弱可分性确立为核心约束;融合投票的诚实发现;数据集局限主动声明(模块 3 + 数据章)

> ⚠️ **本节发现 1、2、3 已被 D-044(修订 16)据实推翻/修正,以下为旧数据(旧 IEC 打标)的历史记录,请勿直接引用**:① 融合「全面差于 iForest」已**翻转**——改用 DL/T 722 表7 后融合 F1 0.649>iForest 0.608;② 「弱可分是统一根因」**不再成立**——表3 二分打标后数据可分性合理(多数气体 d≥0.43、C₂H₆ 0.96);③ 发现 3 的「反证」前提(原始 IEC 打标 C₂H₂ d=0.00)随打标方法改变而失去意义。最新结论见修订 16 / D-044。

- **背景**:实施期审视检测模块时跑数,发现并追根因,形成一组贯穿全文的诚实发现(D-039/D-040)
- **发现 1 — 等权融合投票未优于单一 iForest**:三方法(阈值/IEC/iForest)≥2 票融合,实测 acc=0.756/F1=0.569/fpr=0.204,**全面差于单一 iForest**(0.803/0.608/0.130),仅召回略高。原因:弱方法(IEC 召回 0.407、阈值法 fpr 0.454)进等权投票稀释了 iForest。**论文据实呈现,不宣称「融合更优」**;指出「等权投票受弱方法拖累,加权融合为未来方向」。检测前端「近7日一致性」相应改为「多方法一致性观察」叙事
- **发现 2 — 弱可分性是统一根因**:检测三方法 60-80%、IEC 召回 40%、融合未优、预警 70% 误报(阶段五),**同一根因=数据正常/异常气体分布高度重叠**。量化(类 Cohen's d):CO 0.11/C₂H₂ 0.29/C₂H₄ 0.41/H₂ 0.47/CH₄ 0.52/C₂H₆ 0.60/CO₂ 0.80,除 CO₂ 外均弱可分;异常组总烃中位数仅 90(国标注意值 150),**66% 异常样本未超国标**——合成的是**早期/轻微潜伏故障**,正是 DGA 早期诊断的行业难点。ARIMA 优于 LSTM(修订 8)是相关但独立根因(360 天小样本)
- **发现 3 — 弱可分非合成失真(关键反证)**:原始 743 行经 IEC 打标后可分性**更低**(C₂H₂ d=0.00、H₂ 0.11),合成数据可分性反而略高 → 证明弱可分是**数据集与 DGA 问题的固有性质**,非合成器引入。**故不调整合成器**(硬把异常调显著=脱离原始分布失真,违 P1 诚实原则 D-023)
- **数据集局限主动声明(写入数据章)**:本研究公开 DGA 数据集(bmrayan.github.io/dgadb,743 行)经查**无正式论文、无标注方法说明、来源未标注**,原始 `Fault` 列 97.6% 缺标。**主动声明**此局限,并说明弥补:① IEC 60599 自动打标 + 原始 18 有效标签交叉验证;② 量化数据可分性;③ 合成保持原始分布。立场:本科毕设以**全链路方法验证**为目标,公开数据约束下**如实呈现方法表现**,以学术诚实补数据不完美
- **影响**:模块 3 评估叙事增「融合诚实发现」;数据章增「数据集局限声明 + 可分性分析」;创新点/方法链不变。与修订 8(ARIMA>LSTM)、修订 9(评估基准 fault_state)、阶段五误报诚实(D-033)共同构成**「据实呈现合成数据约束下方法表现」的统一基调**——这是本研究区别于「调参刷指标」的严谨性所在

---

### 修订 13:模块 6 LangChain Agent 实施落地——预跑落盘 + 调度形态调整(D-041)

- **创新点不变**:Agent 以 ReAct 模式端到端串联「取数→检测→预测→规则」全链路、LLM 生成预警通知,仍是本研究最大创新点。4 个 `@tool`、ReAct Prompt 写死 5 步、降级处理均按论文规划落地(通义千问 ChatTongyi + LangChain create_react_agent,ReAct 实测约 13s 跑通)
- **调度形态调整(原「定时调度」→「预跑离线触发」)**:论文原写「AgentExecutor + 定时调度」。实测一次 ReAct 调 LLM 约 12-14s,不宜进 HTTP 请求路径(承修订 7 / D-027「在线轻量」原则),故改为 `run_agent_demo.py` 离线对代表性工单预跑、落盘 `agent_runs.json`,前端只读快照秒开。定时器未实装(答辩可补),性质同修订 7「离线训练+在线推理」——**重活离线、在线只读**
- **边界双保险(强化 D-008)**:Agent 的 Final Answer(预警通知)是 LLM 自由生成,最易越界。除 Prompt 明文禁故障类型/健康评分/运维建议/置信度外,落盘前再过关键词黑名单,命中即作废回退模板通知。**实测捕获过一次 LLM 越界**(某次通知含「停运」被拦 → 降级),印证守边界不能只靠 Prompt 自觉、需代码兜底——可作为答辩「如何守住系统边界」的实证
- **可视化承接修订 10**:轨迹经 `/api/agent/run` 接入 AlertsView 工单详情(点单追溯),据实接真、去除此前的示意数据标识;预跑未覆盖的工单据实显「未预跑」占位(承 P1 诚实原则,不杜撰)

---

### 修订 14:预警 message 由笼统话术改为带确切数值的可解释信息(模块 5,D-042)

- **背景**:模块 5 预警 message 原为笼统话术(「C₂H₂ 当前值已超国标注意值」「预测未来 N 天将超注意值」「呈明显上升趋势」),缺确切数值,运维拿到预警不知「超了多少、几天后到多少、涨多少」,可解释性不足
- **修订**:message 改为模板化填入引擎已有的真实数值——硬规则给「实测值 + 注意值」(如 `C₂H₂ 当前值 8.2 μL/L 已超国标注意值 5 μL/L`)、软规则给「第几天 + 预测值 + 注意值」、趋势规则给「涨幅% + 前后值」、组合规则给「气体 + 涨幅 + 油温」。注意值统一标注国标法定单位 μL/L
- **不动判定、不动指标**:仅 message 文案增强,规则判定逻辑/阈值/分级全未变;回测指标(修订 9 的误报率/召回/F1)不受影响
- **守边界(系统刚性边界)**:message 仍只输出「哪个气体 / 超了多少 / 几天后 / 涨多少」等中性量化信息,**不出现任何故障类型词**,合规
- **意义**:强化预警的「可解释性」——这是预警系统区别于黑箱告警的关键,也是答辩展示「预警如何被推导」的直观素材

---

### 修订 15:DL/T 722 引用口径校准——单位 μL/L、声明 220kV 电压前提、CO/CO₂ 正名为辅助阈值(D-043)

- **背景**:核查全项目对 DL/T 722 的引用是否准确。结论:核心注意值数值与口径正确(H₂=150/C₂H₂=5/总烃=150 μL/L、总烃合并判、产气速率作特征有据),但有三处措辞/出处需校准
- **校准(不动数值与逻辑)**:① 单位由 ppm 规范为国标法定单位 **μL/L**(等价 ppm);② **明确声明设备电压等级为 220kV 及以下**——注意值随电压分档(尤其 C₂H₂:220kV 及以下=5、330kV 及以上=1),论文须声明此前提,否则注意值取值无依据;③ **CO/CO₂ 正名**:DL/T 722-2014 表 1 仅规定 H₂/总烃/C₂H₂ 三项注意值,未设 CO/CO₂ 注意值(国标对固体绝缘走 CO₂/CO 比值判老化),本系统 CO=300/CO₂=10000 为工程经验辅助阈值,不应称「国标注意值」
- **三比值法不改**:系统采用 IEC 60599 编码,DL/T 722-2014 为其国标本土化版本、编码逻辑同源;三比值法只供内部异常二分类(对外不输出故障类型),改编码将推翻已定稿的全部对比数据,得不偿失。论文如实表述二者关系即可
- **答辩防雷点**:第 4 章阈值法须写明「设备设定为 220kV 及以下」「CO/CO₂ 为辅助监测阈值而非国标注意值」,避免评委追问 CO 阈值出处或设备电压时失据

---

### 修订 16:三比值法改用 DL/T 722-2014 表7 + CO/CO₂ 改比值法 + 打标改表3二分 + 全链路重跑(D-044)

> **本条更新/修正了修订 3、8、12、15 的部分结论**(对照官方扫描页逐格核对后,据实推翻)。

- **背景**:修订 15 曾结论「三比值法不改(IEC 与国标同源)」。借官方 DL/T 722-2014 扫描页(表3/表6/表7)逐格核对后发现:① 注意值在**表3 不是表1**(修订15 误写);② 表7 故障归类与 IEC 60599(附录B)**不是同一套**(表7 首位2=低能放电/1=电弧放电,(0,0,0)=低温过热);③ 项目题为「基于 DL/T 722」,三比值法却用 IEC 编码,口径不一致。用户要求「按官方的来,只要最正确,多大改动都可以」,故执行全栈对齐。
- **改动(全链路 X)**:
  1. **三比值法 IEC→DL/T 722 表7**(`detect/iec.py` 重写故障归类,27 编码逐格验证;正名「三比值法(DL/T 722)」,IEC 降为编码同源的国际标准交叉印证);
  2. **CO/CO₂ 改 §10.2.3.1 比值法**:删 `threshold.py` CO/CO₂ 绝对阈值(表3 未设)+ 删预警 H-04/H-05/S-04 + CO=900 校准;新增组合规则 **C-02**(「已有特征气体超注意值」且「CO₂/CO<3」→ 提示关注固体绝缘,守边界 D-008 不输出故障类型);
  3. **ground truth 打标改「表3 二分 + 表7 细分」**(根除修订3 隐含的方法论错误「用三比值法判健康」):健康/异常 = 表3 注意值;异常类型 = 表7;判不出的异常 = 「异常-未明类」单独成池;
  4. **全链路重跑**:重新打标→重新合成(健康74.7%/异常25.3%)→重新入库→重建特征→检测对比→回测→重训 LSTM→预测对比。
- **真实新数字**(全部经磁盘/前台重跑亲核):
  - 检测三方法:阈值法(表3)F1 0.659/召回0.670/误报0.123;三比值法(表7)召回**0.813**/误报0.494/F1 0.497;iForest F1 0.608。
  - 融合(≥2票)F1 **0.649**,**优于 iForest(0.608)**、与阈值法(0.659)相当。
  - 预警回测:误报率 **73.2%**、召回0.65(TP52/FP183/FN28/TN67)。
  - 预测:ARIMA 胜 6/7,总体 MAE 148.3 vs LSTM 160.4(旧为全胜7/7、301 vs 936)。
  - 可分性:co0.14/h20.21/c2h2 0.43/c2h4 0.61/ch4 0.69/co2 0.81/c2h6 0.96;异常总烃中位 179(>150)、未超国标占比 66%→41%。
- **⚠️ 推翻修订12「弱可分统一根因」+ 修订8 程度 + 修订12「融合未优」**(本条最重要的诚实修正):
  - **「数据弱可分(d多<0.5)是统一根因」不成立**:新打标(表3 定义异常=气体确实偏高)下数据明显更可分(5/7 气体 d≥0.43、c2h6 0.96)。原「弱可分」很大程度是旧 IEC 打标(把低值仅比值异常样本也标异常)人为造成。残余误判改归因「早期/轻微异常边界样本 + 预警预测提前量」。
  - **「IEC 召回40%=国标局限」彻底作废**:三比值法表7 召回 0.813。新叙事:三方法精度/覆盖/均衡互补。
  - **「融合未优于 iForest」翻转**:三比值法不再拖累,融合 F1 0.649>iForest 0.608。
  - **ARIMA>LSTM 程度减弱**:从「全面碾压(301 vs 936)」改为「略优(148 vs 160,胜6/7)」,结论方向不变。
- **守边界 D-008**:三比值法表7 故障类型仍仅内部用(ground truth/状态池),对外只 is_abnormal;C-02 message 只提示「关注固体绝缘」。
- **诚实声明(论文须写)**:ground truth 由国标 DL/T 722-2014 打标定义(表3二分+表7细分),检测三比值法亦用表7——评估口径自洽;原始数据 97.6% 缺真实标签是客观局限,任何自动打标都带方法取向,本文据实呈现。
- **核验**:pytest 61 passed;27 编码逐格验证;FAULT_TRANSITION_PROBS 键与状态池全匹配;backtest 前台重跑亲核 73.2%(此前一次「67.1%」系桥接注入假值已纠正)。详见 D-044。

---

### 修订总览

| 编号 | 项目          | 原规划          | 修订后             | 性质       |
| :--: | ------------- | --------------- | ------------------ | ---------- |
|  1   | 数据库        | MySQL           | SQLite             | 选型务实   |
|  2   | 时序数据来源  | 假设有真实时序  | 分布保持的时序构造 | 适应数据   |
|  3   | Ground truth  | 数据集 Fault 列 | IEC 60599 自动诊断 | 反而更严谨 |
|  4   | 工况数据      | 数据集中读取    | 模拟生成           | 适应数据   |
|  5   | H₂O 列        | 保留            | 删除               | 数据驱动   |
|  6   | LSTM 目标气体 | 7 种同步        | 7 种(预案降级 5+2) | 风险预案   |
|  7   | 模块 4 部署   | 未显式描述      | 离线训练+在线推理  | 架构显式化 |
|  8   | LSTM vs ARIMA | LSTM 精度占优   | ARIMA 实测全面更优 | 数据驱动实证 |
|  9   | 评估基准(检测/预警) | IEC 参照(修订3)| 合成真值 fault_state(D-020/033)| 口径统一更严谨 |
|  10  | Agent 可视化承载 | 未明确承载页 | 嵌 Alerts 工单详情点单追溯(D-035)| 形态明确 |
|  11  | AnalysisView 数据 | 三层体系静态原型 | 三层体系据实接真+补特征展示(D-038)| 据实+守边界 |
|  12  | 检测融合/数据基础 | 假设融合更优·数据无声明 | 据实:融合未优于iForest+弱可分统一根因+数据局限主动声明(D-039/040)| 据实+诚实 |
|  13  | 模块6 Agent 调度 | AgentExecutor+定时调度 | 预跑离线触发+落盘只读(承在线轻量)+边界黑名单兜底(D-041)| 工程务实+守边界 |
|  14  | 预警 message 形态 | 笼统话术(无数值)| 模板填入确切数值:实测/注意值/预测值/第几天/涨幅前后值(D-042)| 提升可解释性 |
|  15  | DL/T 722 引用口径 | ppm·未声明电压·CO/CO₂称国标 | μL/L·声明220kV及以下·CO/CO₂正名辅助阈值(D-043)| 引用严谨 |
|  16  | 三比值法标准+打标+CO/CO₂ | IEC编码·三比值法判健康·CO/CO₂绝对阈值·弱可分统一根因 | 三比值法改DL/T722表7·表3二分打标·CO/CO₂改§10.2.3.1比值法·全链路重跑(D-044)| 全栈对齐国标+据实修正 |

**核心结论**:目标、系统架构、4 大核心能力、4 个模块的关键算法均无变更;修订主要发生在"数据来源描述"与"评估方法学"层面,每项均有数据驱动依据。**创新点 2 正文已重写(修订 8,方案 A)**——改为对比研究叙事,据实呈现 ARIMA 在小样本高波动 DGA 合成数据上较 LSTM 更优的实测结果(D-044 重跑后为「略优、胜6/7」,结论方向不变),价值落在为算法选型提供实证依据。**评估方法学经 D-044 全栈对齐国标 DL/T 722-2014**(三比值法表7、表3二分打标、CO/CO₂比值法),并据实修正了「弱可分统一根因」「IEC召回低=国标局限」「融合未优」等早期结论——详见修订 16。
